# Narender Singh Kang — Portfolio

Single-page React + Vite portfolio built from the supplied CV and project links.

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

## Deploy to Vercel
Import this project/repository into Vercel. Framework preset: Vite. Build command: `npm run build`; output directory: `dist`.
